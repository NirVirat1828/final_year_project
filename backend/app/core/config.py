from __future__ import annotations

# Define the single source of truth for the active model version.
# In a real environment, this might be loaded from an environment variable or artifact metadata.
MODEL_VERSION = "1.0.0"
