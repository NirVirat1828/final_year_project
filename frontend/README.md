# Frontend

This folder contains the React dashboard for the orange freshness project.

## Run locally

```bash
cd frontend
npm install
npm run dev
```

## Build

```bash
cd frontend
npm run build
```

The dashboard is a presentation layer for the project structure, dataset map, workflow, and results. It does not replace the Python training or inference scripts.
